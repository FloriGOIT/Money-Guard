import style from "../moneyGuard.module.scss"


const BigBtnNoColorAll = ({valueBtn}) => {
        return <button className={style.bigBtnNoColor}>{valueBtn}</button>
}

export default BigBtnNoColorAll